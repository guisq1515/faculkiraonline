import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.centrouniversitario.app',
  appName: 'Centro Universitário',
  webDir: 'dist'
};

export default config;
